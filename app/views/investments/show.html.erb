<!DOCTYPE html>
<html>
<head>
<style>
.content-loader table {
  font-family: arial, sans-serif;
  border-collapse: collapse;
  white-space: nowrap;
}

td, th {
  border: 1px solid #dddddd;
  text-align: center;
  padding: 8px;
}

tr:nth-child(even) {
  background-color: #dddddd;
}
</style>
</head>
<body>

<h2>Investment Details:</h2>
<p><%= link_to 'Welcome Page', welcome_path %></p>
<p><%= link_to 'All Client Investments',client_investments_path(@client,@investments) %>
<table>
  <tr>
    <th>Investment</th>
    <th>EIN</th>
    <th>Ordinary Income</th>
    <th>Interest Income</th>
    <th>Short-Term Capital Gains/(Losses)</th>
    <th>Mid-Term Capital Gains/(Losses)</th>
    <th>Long-Term Capital Gains/(Losses)</th>
    <th>Total Income</th>
    <th>Edit</th>
    <th>Delete</th>
    <th>Reportable</th>
  </tr>
  <tr>
    <td><%=@investment.name%></td>
    <td><%=@investment.ein%></td>
    <td><%=number_to_currency(@investment.ordinary_income, :unit => "$")%></td>
    <td><%=number_to_currency(@investment.interest_income, :unit=> "$")%></td>
    <td><%=number_to_currency(@investment.st_capital,:unit => "$") %></td>
    <td><%=number_to_currency(@investment.mt_capital, :unit => "$")%></td>
    <td><%=number_to_currency(@investment.lt_capital, :unit=> "$")%></td>
    <td><%=number_to_currency(@investment.total, :unit => "$")%></td>
    <td><p><%= link_to("Edit", edit_client_investment_path(@client,@investment)) %></p></td>
    <td><p><%= link_to("Delete", client_investment_path(@client,@investment), method: :delete, data:{confirm: "Are you Sure?"}) %></p></td>
    <td><p><%= @investment.reportable_loss%></p></td>
    </tr>
  <br>


