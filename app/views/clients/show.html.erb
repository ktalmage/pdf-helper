<h1>Client Info: </h1>
<p><%= @client.name %></p>
<p><%= @client.ein %></p>

<p> <%= link_to("Edit", edit_client_path(@client)) %></p>
<p> <%= link_to("Delete", @client, method: :delete, data:{confirm: "Are you Sure?"}) %></p>

<h2>Client's Investments</h2>

 <%= link_to "Add an Investment", client_investment_path %>
 <%# <table>
  <tr>
    <th>Investment</th>
    <th>EIN</th>
    <th>Ordinary Income</th>
    <th>Interest Income</th>
    <th>Short-Term Capital Gains/(Losses)</th>
    <th>Mid-Term Capital Gains/(Losses)</th>
    <th>Long-Term Capital Gains/(Losses)</th>
    <th>Total Income</th>
    <th>Edit</th>
    <th>Delete</th>
  </tr>
  <tr> %>
    <% @client.investments.each  do |inv|%>
    <%= inv.name %>

    <% end %>
<%# </tr> %>
    
     <%# <td><%=@client.investments.ein</td>  %>
     <%# <td><%=number_to_currency(@client.investments.ordinary_income, :unit => "$")%>%></td>
     <%# <td><%=number_to_currency(@client.investments.interest_income, :unit=> "$")%></td>
    <%# <td><%=number_to_currency(@client.investments.st_capital,:unit => "$") %></td> 
    <%# <td><%=number_to_currency(@client.investments.mt_capital, :unit => "$")%></td> %>
     <%# <td><%=number_to_currency(@client.investment.lt_capital, :unit=> "$")%></td> %> %>
    <%# <td><%=number_to_currency(@investment.total, :unit => "$")%></td> %> %>
     <%# <td><p><%= link_to("Edit", edit_investment_path(@investment)) %></p></td>
   <%# <td><p><%= link_to("Delete", @investment, method: :delete, data:{confirm: "Are you Sure?"}) %></p></td> %>
   </tr> %>
   <br>
